
/*projects*/

.projects{
    position: relative;
    margin-bottom: 120px;

    &__title{
        margin-bottom: 90px;
    }
    
    &__content{
        display: flex;
        flex-direction: row;
    }
    
    &__left{
        flex: 3;
        padding-right: 150px;
        position: relative;
    }
    
    &__right{
        flex: 1;
        display: flex;
        flex-direction: column;
        color: #7a7f8f;

        .projects__skills{
            &-list{
                display: flex;
                flex-direction: row;
                margin-bottom: 46px;
            }
            
            &-item{
                margin-right: 15px;
                border-radius: 23px;
                background-color: #f3f5f9;
                padding: 14px 20px;
                font-size: 15px;
                font-weight: 600;
            
                &:last-child{
                    margin-right: 0;
                }
            }
        }

        .projects__right-title h4{
            font-size: 30px;
            font-weight: 700;
            color: #464d62;
            margin-bottom: 40px;
        }
        
        .projects__text p{
            font-weight: 600;
            margin-bottom: 45px;
            line-height: 30px;
        }
        
        .projects__link a{
            font-size: 18px;
            color: #586fd3;
            font-weight: 700;
            position: relative;
            margin-left: 35px;

            &:hover{
                text-decoration: underline;
            }
        }
    }

    &__figure{
        position: absolute;
        top: -45px;
        right: 0;
        font-size: 400px;
        font-weight: 800;
        color: #e6e6e6;
        z-index: -1;
    }
}

.projects__link-icon{
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    left: -35px;
    width: 15px;
    height: 15px;
    fill: currentColor;
}

.projects__slider{
    width: 990px;
    height: 560px;
    overflow: hidden;
    position: relative;

    .projects__photo{
        max-width: 100%;
    }

    &-item{
        width: 100%;
    }
    
    &-links{
        position: absolute;
        bottom: 0;
        right: 160px;
    }
    
    &-list{
        display: flex;
        flex-direction: column;
        position: relative;
    
        &-item{
            width: 178px;
            height: 100px;
            position: relative;

            .projects__slider-list-img{
                width: 100%;
            }

            &:hover .projects__slider-img-cover{
                opacity: .6;
            }
        }
    }
    
    &-btns{
        display: flex;
        flex-direction: row;
        position: absolute;
        bottom: -47px;
        right: 3px;
        color: #fff;
    
        &-link{
            background: #353e4e;
            padding: 14px 18px;
            transform: rotate(90deg);

            &:hover{
                background: #dc9322;
            }
        }
    }
}

.projects__slider-list-item--active .projects__slider-list-img{
    outline: 2px solid #dc9322;
}

.projects__slider-img-cover{
    width: 100%;
    height: 100%;
    background: #dc9322;
    z-index: 3;
    position: absolute;
    top: 0;
    left: 0;
    opacity: 0;
    will-change: opacity;
    transition: 0.5s;
}